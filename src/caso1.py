# casos em python 
# criar função dinamica em python para gerar consultas e recuperar dataframes (ou seja, retornar sempre um dataframe)
# precisamos de uma função que recebe : 
# product_code (codigo do produto): int
# (codigo da loja) store_code: int
# (data) date: list
# retornar todas as colunas de data_product_sales

# ex.: retrive data( product_code = x, store_code = 10, date = none) retorna tudo daquela loja